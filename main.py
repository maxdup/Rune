from rune.database import Database
from rune.schema import Schema

db = Database('db.rune')
'''
schema1 = Schema(1, ['int', 'uint', 'obj', 'str'])
schema2 = Schema(1, ['int', 'str'])
schema3 = Schema(1, ['int', 'obj', 'str'])

db.add_schema(schema1)
db.add_schema(schema2)
db.add_schema(schema3)
'''

